#Lab1 

# Customer information examples

customer_id = 1001 # I used a number because customer IDs are often numeric, but some companies may use letters too.
customer_name = "Jeanette Her" # I wrote first name + last name together, but it might be better to separate them.
customer_gender = "Female" # I used a word, but the system might use abbreviations like "F", "M", or another option.
customer_date_of_birth = "1995-04-12" # I used a string in YYYY-MM-DD format, but Python also has a date type.
drivers_license_number = "D1234567" # I used a string because license numbers can include both letters and numbers.
auto_policy_number = "AP-987654" # I used a string because policy numbers may include letters, numbers, or dashes.

# My information

my_name = "Mohamed Mouatakid" # I used my full name as one string, but first and last name could be separate variables.
birth_city_state = "Tinghir, Morocco" # I used city and location together, but they could be stored in separate variables.

# Lab 2

#a)
# Reserved words in Python cannot be used as variable names.

# False, None, True, and, as, assert, break, class, continue, def, del, elif, else,
# except, finally, for, from, global, if, import, in, is, lambda, nonlocal, not,
# or, pass, raise, return, try, while, with, yield

#b)
# def: This keyword is used to define a function or method in Python.
# if: This keyword is used to make a ^^conditional^^ statement. If the condition is True, an action runs.
# import: This keyword is used to bring a module or library into a Python program.
# lambda: This keyword creates an ^^anonymous function^^, which is a small function without a regular name.
# raise: This keyword is used to raise an error, which can appear in the ^^traceback^^ if the program fails.

# The article notes that these words have special meaning in Python and cannot be used as variable names, function names, or other identifiers

